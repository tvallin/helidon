
server:
  host: 0.0.0.0
  port: 8080

mcp:
  server:
    name: "mcp-server"
    version: "0.0.1"
    capabilities:
      tool:
        list-changed: "false"
      prompt:
        list-changed: "true"
      resource:
        list-changed: "true"
        subscribe: "true"
